
# This file controls the auditd data path to the
# remote event logger. This plugin will send events to
# a remote machine (Central Logger). Its syntax is
# explained in auditd-plugins(5)

active = no
direction = out
path = /sbin/audisp-remote
type = always
#args =
format = string
